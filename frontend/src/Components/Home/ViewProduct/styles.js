import {makeStyles} from "@material-ui/styles";

export const useStyles = makeStyles(() => ({
  rootDiv: {
    margin: "24px",
  },
  title: {
    marginBottom: "12px",
  },
  displayFlex: {
    display: "flex",
    justifyContent: "center",
  },
}));

import { makeStyles } from "@material-ui/styles";

export const inputStyles = makeStyles(() => ({
  root: {
    backgroundColor: "#fff",
  },
  focused: {},
  notchedOutline: {
    border: "none",
  },
}));
